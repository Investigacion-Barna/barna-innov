# Datos sensibles de la investigación — contienen respuestas y emails reales
*.xlsx
*.xls
*.csv

# Volcados temporales y scratch
xlsx_extracted/
csv_out*/

# Reportes personalizados por encuestado · contienen emails y respuestas reales
reportes/

# Llaves SSH · NUNCA subir al repo
*.ppk
*.pem
id_rsa*
id_ed25519*
*_SSH_Priv*

# Logs y editores
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node (por si se añade tooling después)
node_modules/

# Configuración local de Claude Code (no útil para el repo)
.claude/

# LibreOffice lock files
.~lock.*
